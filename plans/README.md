# Implementation Plans

Generated by the `improve` skill (roadmap/direction pass) on 2026-07-08.
Execute in the order below. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Context

boop repositioned **agent-first** on 2026-07-08 (`docs/business-plan.md`,
`src/pages/ApiQuickstart.tsx`). The stack already has a live JWT-authed REST API
(`/api/lists/*`, `/api/items/*`, …), but agents can't use it unattended: no
durable credential and no read endpoint. The full "Mission Control" surface
(runs, memory sync, dashboards) was **deliberately removed** on 2026-03-05
(`90b6d7b`) during a consumer-freemium pivot — these plans do **not** resurrect
it. They add the minimal slice that makes the shipped landing page honest and
lets agents genuinely use the current stack.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Agent access slice: API keys + read/write over HTTP | P1 | L | — | DONE (steps 0–4,6 on branch `feat/agent-api-keys`; step 5 attribution deferred). `tsc`/`bun test`/`bun run build` green; smoke-tested end-to-end on `dev:basic-bloodhound-21`. |

## Dependency notes

- 001 is self-contained. Its steps are internally ordered (helpers → schema →
  resolver → read endpoints → optional attribution → docs); step 5 (attribution)
  is optional and may ship as a separate follow-up PR.

## Findings considered and rejected / deferred

- **Resurrect PR #167 (full Mission Control)**: not worth doing now. It was
  removed by deliberate product decision, is 4 months / 63 commits stale, forked
  before the removal, and depends on Playwright E2E (against repo policy). Its
  orchestration/observability surface (runs, heartbeats, dashboards, memory sync)
  is not required for "agents can use boop." Revisit only if the agent GTM gains
  traction and the observability story becomes a sale-blocker.
- **VC proof signing**: real, but out of scope for the access slice — item VCs
  are unsigned placeholders (`convex/items.ts:11`), blocked on the DID-signing
  follow-up. Tracked separately; do not bundle into 001.
- **Make the landing page copy honest** (`ApiQuickstart.tsx`): partially
  addressed by 001 step 6 (API.md). If 001 slips, gate the agent-first claims
  behind a "beta" label as a cheap standalone fix.
